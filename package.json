{
  "name": "obsidian-routines-plugin",
  "version": "0.1.0",
  "description": "An Obsidian plugin for managing custom routines with optional logging.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -noEmit && node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.17.14",
    "esbuild-svelte": "^0.9.0",
    "svelte": "^4.2.1",
    "svelte-preprocess": "^5.0.0",
    "typescript": "^5.1.6"
  }
}
